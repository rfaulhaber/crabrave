#+title: Todo

API doc: https://www.tumblr.com/docs/en/api/v2
NPF doc: https://www.tumblr.com/docs/npf

* DONE OAuth1 Authorization
- HMAC-SHA1 signature generation
- Automatic Authorization header construction
- Support for consumer key/secret and access token/secret
* DONE OAuth2 Authorization
- OAuth2Config for managing auth flow
- Authorization URL generation with PKCE support
- Token exchange and refresh functionality
- Bearer token authentication
* DONE Move post methods to under blog endpoints
#+begin_src rust
crab.blogs("staff").posts().send() // fetches multiple posts
crab.blogs("foo").post("123").get() // fetches single post
crab.blogs("foo").create_post().content(/*...*/).send() // makes new post
crab.blogs("foo").post("123").delete() // delete post
#+end_src
* Blog [25/25]
** DONE /info - Retrieve Blog Info
** DONE /avatar — Retrieve a Blog Avatar
** DONE /blocks – Retrieve Blog's Blocks
** DONE /blocks – Block a Blog
** DONE /blocks/bulk – Block a list of Blogs
** DONE /blocks – Remove a Block
- Supports both blog unblock and bulk anonymous unblock
** DONE /likes — Retrieve Blog's Likes
- Should be a blog method
** DONE /following — Retrieve Blog's following
- Should be a ~blog~ method
** DONE /followers — Retrieve a Blog's Followers
** DONE /followed_by — Check If Followed By Blog
** DONE /posts – Retrieve Published Posts
** DONE /posts/queue — Retrieve Queued Posts
** DONE /posts/queue/reorder — Reorder Queued Posts
** DONE /posts/queue/shuffle — Shuffle Queued Posts
** DONE /posts/draft — Retrieve Draft Posts
** DONE /posts/submission — Retrieve Submission Posts
** DONE /notifications — Retrieve Blog's Activity Feed
** DONE /posts - Create/Reblog a Post (Neue Post Format)
- Uses NPF content blocks for create and reblog
- Supports media uploads via multipart/form-data
- MediaSource type for file paths or byte data
- Auto-generated identifiers with add_image() and add_video() convenience methods
- Automatic MIME type detection from file extensions
** DONE /posts/{post-id} - Fetching a Post (Neue Post Format)
- Implemented as ~crab.blogs("foo").post("123").get()~
- Part of BlogPost handler with get(), delete(), edit(), mute() methods
** DONE /posts/{post-id} - Editing a Post (Neue Post Format)
- Uses PUT with NPF content blocks
- Supports media uploads via multipart/form-data (same as create)
- Can add new images/videos when editing posts
** DONE /post/delete – Delete a Post
** DONE /posts/{post-id}/mute - Muting a Post's Notifications
** DONE /notes - Get notes for a specific Post
** DONE /pages - Retrieve Pages
** DONE /pages/{page-name} - Retrieve a Page
* User [11/11]
** DONE /user/info – Get a User's Information
** DONE /user/limits – Get a User's Limits
** DONE /user/dashboard – Retrieve a User's Dashboard
** DONE /user/likes — Retrieve a User's Likes
** DONE /user/following – Retrieve the Blogs a User Is Following
** DONE /user/follow – Follow a blog
** DONE /user/unfollow – Unfollow a blog
** DONE /user/like – Like a Post
** DONE /user/unlike – Unlike a Post
** DONE /user/filtered_tags - Tag Filtering
** DONE /user/filtered_content - Content Filtering
* Tagged [1/1]
** DONE /tagged – Get Posts with Tag
* Communities [19/20]
All endpoints use correct paths (=/communities/=) with proper HTTP methods.
** DONE GET /v2/communities - List joined communities
- Implemented as ~crab.users().joined_communities()~
** DONE GET /v2/communities/{community-handle} - Get info about a community
- Implemented as ~crab.communities("handle").info()~
** TODO PUT /v2/communities/{community-handle} - Edit your community
- Not yet implemented (admin-only endpoint)
** DONE GET /v2/communities/{community-handle}/invitations - View pending invites
- Implemented as ~crab.communities("handle").invitations()~
** DONE PUT /v2/communities/{community-handle}/invitations - Invite someone to the community
- Implemented as ~crab.communities("handle").invite("blog")~
** DONE DELETE /v2/communities/{community-handle}/invitations/{blog-identifier} - Cancel an invitation
- Implemented as ~crab.communities("handle").cancel_invitation("blog")~
** DONE GET /v2/communities/{community-handle}/invitations/{blog-identifier} - Check invitation status
- Implemented as ~crab.communities("handle").invitation_status("blog")~
** DONE POST /v2/communities/{community-handle}/invite_hash - Regenerate the invite URL
- Implemented as ~crab.communities("handle").regenerate_invite_url()~
** DONE GET /v2/communities/{community-handle}/members - Get community members
- Implemented as ~crab.communities("handle").members().limit(20).send()~
- Uses MembersBuilder for pagination
** DONE PUT /v2/communities/{community-handle}/members - Join a community
- Implemented as ~crab.communities("handle").join()~
** DONE DELETE /v2/communities/{community-handle}/members/{blog-identifier} - Leave/remove a member
- Implemented as ~crab.communities("handle").leave("blog")~ (for self)
- Implemented as ~crab.communities("handle").remove_member("blog")~ (for moderators)
** DONE PUT /v2/communities/{community-handle}/members/{blog-identifier} - Change a member's role
- Implemented as ~crab.communities("handle").change_role("blog", MemberRole::Moderator)~
** DONE DELETE /v2/communities/{community-handle}/mute - Unmute a community
- Implemented as ~crab.communities("handle").unmute()~
** DONE PUT /v2/communities/{community-handle}/mute - Mute a community
- Implemented as ~crab.communities("handle").mute()~
** DONE PUT /v2/communities/{community-handle}/posts/{post-id}/reactions - Add reaction to post
- Implemented as ~crab.communities("handle").add_reaction("post_id", "reaction_id")~
** DONE DELETE /v2/communities/{community-handle}/posts/{post-id}/reactions/{reaction-id} - Remove reaction from post
- Implemented as ~crab.communities("handle").remove_reaction("post_id", "reaction_id")~
** DONE GET /v2/communities/{community-handle}/moderation/posts/{post-id} - View a moderated post
- Implemented as ~crab.communities("handle").moderated_post("post_id")~
** DONE DELETE /v2/communities/{community-handle}/moderation/posts/{post-id} - Moderate a post
- Implemented as ~crab.communities("handle").moderate_post("post_id")~
** DONE PUT /v2/communities/{community-handle}/moderation/posts/{post-id} - Restore a post
- Implemented as ~crab.communities("handle").restore_post("post_id")~
** DONE GET /v2/communities/{community-handle}/timeline - Get posts from a community
- Implemented as ~crab.communities("handle").timeline().limit(20).send()~
- Uses TimelineBuilder for pagination
* Media Upload Implementation [1/1]
** DONE Media Upload for Posts
- New ~media.rs~ module with MediaSource type
- MediaSource supports:
  - Creation from file paths: ~MediaSource::from_path()~
  - Creation from bytes: ~MediaSource::from_bytes()~
  - Auto-detection of MIME types from file extensions
  - Manual MIME type override: ~.with_mime_type()~
  - Support for images (jpg, png, gif, webp, etc.)
  - Support for videos (mp4, webm, mov, etc.)
  - Support for audio (mp3, ogg, wav, flac, etc.)
- NPF MediaObject updated with ~identifier~ field
- Multipart/form-data implementation:
  - ~post_multipart()~ for creating posts with media
  - ~put_multipart()~ for editing posts with media
  - Follows Tumblr API format: "json" field + media fields by identifier
- Builder methods:
  - ~.media_source(identifier, source)~ - explicit identifier control
  - ~.add_image(source)~ - convenience method with auto-generated IDs
  - ~.add_video(source)~ - convenience method for videos
  - ~.add_audio(source)~ - convenience method for audio files
  - Automatic JSON vs multipart selection based on media presence
- Test coverage: 10 new tests for media upload functionality
* API Refactoring [1/1]
** DONE Post Operations Moved to Blog Handler
All post operations now go through ~crab.blogs("blog-name")~ for consistency:
- Removed ~crab.posts()~ accessor from main client
- Removed ~Posts~ struct (was redundant with blog parameter)
- Added ~crab.blogs("foo").post("id")~ - returns BlogPost handler
  - ~.get()~ - fetch the post
  - ~.delete()~ - delete the post
  - ~.edit()~ - returns EditPostBuilder
  - ~.mute()~ - mute notifications
- Added ~crab.blogs("foo").create_post()~ - returns CreatePostBuilder
- Added ~crab.blogs("foo").reblog(id, key)~ - returns ReblogBuilder

Benefits:
- Consistent API: all blog operations start with ~crab.blogs()~
- Blog specified once instead of repeated in each operation
- More natural API: ~crab.blogs("foo").post("123").get()~ vs ~crab.posts().get("foo", "123")~
